import React from 'react';
import { BsPersonGear } from 'react-icons/bs';

export const UserCogIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsPersonGear className={className} />
);