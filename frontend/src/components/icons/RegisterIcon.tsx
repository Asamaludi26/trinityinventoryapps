import React from 'react';
import { BsFileEarmarkCheck } from 'react-icons/bs';

export const RegisterIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsFileEarmarkCheck className={className} />
);
