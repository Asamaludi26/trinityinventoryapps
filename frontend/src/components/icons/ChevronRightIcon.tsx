import React from 'react';
import { BsChevronRight } from 'react-icons/bs';

export const ChevronRightIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsChevronRight className={className} />
);
