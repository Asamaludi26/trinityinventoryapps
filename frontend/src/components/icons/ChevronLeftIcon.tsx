import React from 'react';
import { BsChevronLeft } from 'react-icons/bs';

export const ChevronLeftIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsChevronLeft className={className} />
);
