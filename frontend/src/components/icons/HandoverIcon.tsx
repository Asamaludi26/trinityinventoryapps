import React from 'react';
import { BsArrowLeftRight } from 'react-icons/bs';

export const HandoverIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsArrowLeftRight className={className} />
);
