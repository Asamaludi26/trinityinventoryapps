import React from 'react';
import { BsPaperclip } from 'react-icons/bs';

export const PaperclipIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsPaperclip className={className} />
);
