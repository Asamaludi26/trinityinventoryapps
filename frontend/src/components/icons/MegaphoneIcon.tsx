import React from 'react';
import { BsMegaphoneFill } from 'react-icons/bs';

export const MegaphoneIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsMegaphoneFill className={className} />
);