import React from 'react';
import { BsArrowReturnLeft } from 'react-icons/bs';

export const DismantleIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsArrowReturnLeft className={className} />
);
