import React from 'react';
import { BsChevronDown } from 'react-icons/bs';

export const ChevronDownIcon: React.FC<{ className?: string }> = ({ className }) => (
  <BsChevronDown className={className} />
);
